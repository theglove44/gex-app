# Requires Python 3.10+ (tastytrade SDK uses union type syntax)
pandas
python-dotenv
tastytrade>=8.0
requests
websockets
matplotlib

# Web app dependencies
# Dark theme CSS tested with Streamlit 1.40.0 (see VISUAL_TESTING.md for compatibility notes)
streamlit>=1.40.0
plotly
pytest
